class Solution {
    public int solution(String message) {
        int answer = 0;
        int sum = 0;
        int width = 2;
        for(int i = 0; i < message.length(); i++){
            sum = message.length();
            answer = sum * width;
       
        }
        return answer;
    }
}
