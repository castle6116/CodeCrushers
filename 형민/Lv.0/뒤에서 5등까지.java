import java.util.*;
class Solution {
    public int[] solution(int[] num_list) {
        int[] answer = num_list;
        Arrays.sort(num_list);
        return  answer = Arrays.copyOfRange(answer, 0, 5);
    }
}